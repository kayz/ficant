[project]
name = "ficant-node-runtime"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
    "protobuf==6.33.5",
]

[dependency-groups]
dev = [
    "pytest==9.0.3",
]

[tool.uv]
package = false
