# R2 审计报告 — 规则包必须被解析

**审计日期** 2026-07-28 · **被审对象** [`docs/iterations/2026-08-r2-rulepack-parsing.md`](../iterations/2026-08-r2-rulepack-parsing.md) · **候选 base** `29a941c1236dd17e93d7ae4f0745d82b071a4b47`

**审计方式** 源码 / 契约 / 门禁脚本 / 规则包内容的静态实证。**本次未执行任何命令**（Windows PowerShell + sandbox 不可用），§6 全部 exit code 与 test count 标为待 Human 复核，不写成通过。

> **边界声明：** 本文是审计产物，不在 R2 允许写路径内，不计入 R2 候选。

---

## 1. 结论

**技术交付通过，一处流程记录缺失。**

R2 的三条验收条目在结构上全部成立，且实测强于 brief 自述。规则数值确已从 domain 与 C++ 双侧移出并落到可解析的 pack 内容中——**它们移动了，没有蒸发**，我逐个数值对上了。R1 遗留的两处门禁弱点也被顺手补掉。

唯一问题：**§6 的允许写路径在实现期间被就地扩了三条，而 §5 没有任何扩权记录**——与 brief 自身第 248 行冲突。该偏差的**实质影响已复核为干净**（加法式、默认行为保持、Phase 2D hedge 未接入），但记录缺失成立。

**因此 R2 不得被表述为「范围流程完全合规」。** 议定补救为 docs-only 纠正提交，见 §4。

---

## 2. 验收条目判定

### AC01 · 规则数值清零

| 检查 | 判定 | 实证 |
|---|---|---|
| domain 侧清零 | **● 成立** | `futures_delivery.rs` 检索 `original_term_months`、`residual_term_bounds`、`=> 60/84/120/360`、`Some(27)`、`Some(63)` 全部命中为零 |
| C++ 侧清零 | **● 成立** | `cpp/fixed-income-kernel/src` 检索 `bounds = {`、`360, 300`、`original_months`、`minimum_residual_months` 命中为零。R1 审计时 `futures_math.cpp:33` 的 `bounds = {360, 300, 0}` 已消失 |
| allowlist 清空 | **● 成立** | `scripts/layering-allowlist.json` 内容为 `[]` |
| C++/FFI 检查独立且无 allowlist | **● 成立** | `check-layering.ps1:143` 新增 `Get-Phase2CProductionRuleValueViolations`，扫描 `cpp/fixed-income-kernel/src/futures_*`（.cpp/.hpp/.h）与 `include/ficant_kernel.h`；:234–239 的判定路径**完全不查 allowlist**，与走 allowlist 匹配的 `Get-DomainRuleViolations` 分离。六类分类齐备：eligibility-bounds、delivery_months、nominal/standard_coupon、face_quote_basis、rounding-scale、annual_day_basis |

### AC02 · 换 pack 换结果 + 缺项失败关闭

| 检查 | 判定 | 实证 |
|---|---|---|
| 判据物存在 | **● 成立** | `crates/ficant-application/tests/futures_delivery_rule_resolution.rs:34` 函数名与 §6 命令中的 `-- --exact` 选择器逐字一致 |
| 缺项路径稳定 | **● 成立** | 同文件 :30 常量 `context.rule_pack.content.products[product_code=T].residual_min_months`，与 §4.4 契约、§6 切片结果三处一致 |

> R1 审计时这条判据在全仓库不存在。R2 补齐并采用了 RED-first 顺序（§2 闸门 1）。RED 本身我无法复核，属待执行清单。

### AC26 · Phase 2C 逐项不变

| 检查 | 判定 | 实证 |
|---|---|---|
| 规则值完整迁移 | **● 成立（逐值核对）** | `domain-packs/cgb-futures/cgb-futures-v1.json` 承载：TS 60/(18,27)、TF 84/(48,63)、T 120/(78,unbounded)、TL 360/(300,unbounded)、deliveryMonths `[3,6,9,12]`、nominalCoupon `3` scale 2、faceQuoteBasis `100`、rounding 4/7、annualDayBasis 365。**与迁移前 domain `const fn` 及 C++ `bounds` 的数值逐个相同** |
| C ABI 承载规则 | **● 成立** | `ficant_kernel.h:181-207` 的 delivery input struct 新增 11 个规则字段（期限三项 + unbounded 标志、delivery_months 计数与指针、day count、两个 rounding places、annual_day_basis、nominal_coupon、face_quote_basis），`struct_size` 与 `abi_version` 保留 |
| 数值方向自洽 | **● 通过合理性抽查** | §6 报告 v2 将 `nominal_coupon.coefficient` 由 `3` 改为 `4` 后 T basket CF 由 `0.965` 降至 `0.8991`。名义票息（贴现口径）上升时，票息低于该口径的券 CF 必然下降——方向正确，幅度量级合理 |

---

## 3. 本轮闸门与 R1 遗留问题

| 项 | 判定 | 实证 |
|---|---|---|
| 闸门 6 · 四类负向 fixture 齐备 | **● 成立** | `test-layering-check.ps1`：domain 规则值（:62、:122）、C++/FFI 规则值（:104–108 循环）、市场分支（:68/:74/:80/:87/:92/:112/:117）、非空 allowlist（:127）各有真实违规 exit `1` 场景，且每个后接「移除后 exit `0`」 |
| 六类 C++ fixture | **● 成立** | :96–103 六条，与门禁六类分类一一对应 |
| **33 assertions 是否可信** | **● 现在可信** | :49 `$script:AssertionCount++` 在 `Invoke-GateExpect` 内自增，:131 以 `-f $script:AssertionCount` 输出。我独立数了固定调用 21 次 + 循环 6×2 = **33**，与报告数字精确吻合。**R1 审计红旗 5（硬编码断言数）已闭合** |
| **`'C'+'N'` 拼接绕过** | **● 已闭合** | :84–89 新增 concatenated market code 负向 fixture，构造 `market == ("C" + "N")` 并断言 exit `1`。**R1 审计红旗 4 已闭合** |
| 门禁只扩不缩 | **◐ 部分可证** | 排除项正则（:30）在本次会话观察到的所有版本中均未增长；`$SourceRoots` 由具名根扩到 `@('.')`，扩展名新增 C/C++ 全族——均为扩大。**但我无法对 R2 base 做真 diff**，只能就本会话内观察到的状态作证 |
| 闸门 7 · matrix 拆分 | **● 成立** | `tests/phase2c/acceptance-matrix.json` 已拆为 `immutable_facts`（:4）与 `rebaselined_tests`（:11）；`tests/phase2d/acceptance-matrix.json` 保留 `guarded_files` 原 schema（:4），符合「只准改单个 hash」 |
| 闸门 7 · 共享 hash 一致 | **● 成立** | `test_constants_and_layout.cpp` 在 phase2c:17 与 phase2d:15 均为 `86f7f56906262dc9e9cf199afd2cc01db4fc26e62ff494c41cf2976cc1cb16f6`，两侧相同，且已从审计前的 `02aba54e…` 更新 |
| 闸门 10 · 禁止提前定义契约 | **● 成立** | `interface/proto/**` 共 19 个 proto = R1 的 18 个 + 新增 `cgb_futures_rule.proto`。无 position / factor / health / constraint / policy |
| 文档一致性 | **● 成立** | `MANUAL.md:67` 已要求「精确且带 `cgb-futures` 内容的 RulePack」；:81 描述解析、复核项与失败关闭语义；§6 诚实清单中「交割规则数值硬编码在代码里，换规则版本不会改变计算结果」**已删除**，全文再无「硬编码」字样 |

---

## 4. 唯一发现：写路径就地扩权，无决策记录

§6 允许写路径相比**我在 go/no-go 时审阅的版本**新增三条，全部与 Phase 2E live SDK 相关（我无 git，无法对 base 版本做真 diff，此为逐字比对我留存的审阅读数）：

| 行 | 新增内容 |
|---|---|
| 223 | `python/tests/test_rates_sdk_live.py` 的 R2 RulePack binding |
| 227 | `crates/ficant-api/tests/phase2e_sdk_live.rs` |
| 232 | `scripts/check-phase2e-sdk.ps1` |

其中 `scripts/check-phase2e-sdk.ps1` 在 R2 开工前即已存在——即这是对既有文件的修改，而非新建，且原不在冻结清单内。

brief 自身第 248 行写明：

> 若实现必须超出允许路径，Root 必须停止并返回 Human 扩权；**不得先改后补**。

而 §5「需 Human 决策」中**没有任何扩权记录**——它仍写着「本轮不再有待决语义」与「当前无 SPEC、ACCEPTANCE 或 ADR diff 提案」。

**实质判断：** 大概率必要且无害。Phase 2E live SDK 测试绑定冻结 pack 的 SHA-256，pack 一旦引入，该测试必须同步，否则 `check-phase2e-sdk.ps1` 无法通过。这是设计时的遗漏，不是越界冲动。

**流程判断：** 记录缺失。而且**brief 自身在允许写路径内**——写路径清单可以被它约束的那个 agent 就地改写。这是我们开工前在 matrix 层面识别并封堵的同一个自管问题，只是上移到了 brief 这一层，当时没人想到。

**Human 复核结论（2026-07-28）：** 偏差属实，`df72d1c` 与 §5 无记录两点均由本地 Git 确认。R2 **不得再被表述为「范围流程完全合规」**。

**实质影响已复核，偏差是干净的：**

- `python/tests/test_rates_sdk_live.py:103` 的 `rule_pack: rates.ObjectBinding | None = None`，:107 `rule_pack or _object(rule_suffix)`——**可选参数 + 保留原默认行为**。
- 只有交割切片（:330）绑定新 pack；reference bond（:204）、curve（:259）、carry-roll（:277）三个切片的绑定方式未变。
- **Phase 2D hedge 切片（:366–377）仍用旧的通用 `_object(rule_suffix)` 绑定，未接入 CGB pack**，与 §3「Phase 2D hedge 暂不改」及禁写路径一致。
- `scripts/check-phase2e-sdk.ps1:5` 注释确认其 RulePack 由 test-only repository 提供，与 §6 第 205 行描述一致。

即：**加法式、默认保持、外科式限定在交割切片**，未触及 §3 非目标禁止的 Phase 2A/2B/2D/2E 业务语义。

**议定补救（docs-only 纠正提交，不改动已隔离的 `df72d1c`）：**

1. §5 新增一条事后偏差记录：写明扩了哪三条、为何必要（Phase 2E live SDK 绑定冻结 pack 的 SHA-256）、**发现于事后审计而非事前授权**、以及上述实质复核结论。不得表述为「当时已授权」。
2. §6 第 210 行 rebaseline 表述由将来时改为过去时，并写入 `df72d1c`。

**结构性修法（建议落在 `docs/iterations/README.md` 而非仅 R3 brief）：** brief 的 §6 写路径清单随 execution base 一并冻结；实现期间发现需要扩权时，只能新增 §5 条目，不得就地编辑清单。放进 README 才对所有后续轮次生效，不依赖谁记得在 R3 补。

---

## 5. 待 Human 复核清单

我无法执行任何命令。以下**全部为未知**，不得按 §6 记述视为已通过。

### 已由 Human 复核完毕（2026-07-28）

| 项 | 结论 |
|---|---|
| `check.ps1` 与 `check.ps1 -IncludeIntegration` | **均 exit `0`，Human 实际执行。不再是未知** |
| 闸门 8 · rebaseline 提交形态 | **`df72d1c` 存在且只含两份 matrix；Phase 2D 仅替换 `test_constants_and_layout.cpp` 一个 hash（`02aba54e… → 86f7f569…`）。合规** |
| 写路径偏差 | **确认存在，见 §4。R2 不得表述为范围流程完全合规** |

### 仍待复核

| 项 | 复核方式 |
|---|---|
| §6 其余 test count 明细 | 与上述两次门禁运行的原始输出逐项对照 |
| AC02 的 RED 证据 | 编排工具中该次非零 exit code 的原始输出 |
| 冻结事实哈希未变 | 重算 5 个 Phase 2C `immutable_facts` 与 5 个 Phase 2D Golden/Oracle 的 SHA-256，比对 matrix 登记值 |
| Canonical schema hash | 确认仍为 `e804a0be…` |
| 写路径整体合规（偏差之外） | `git diff 29a941c1..HEAD --stat`，逐个文件对照 §6 两份清单 |
| 闸门 12 · 门禁脚本 diff | base-to-candidate 的三份脚本 diff，确认只扩大覆盖 |

---

## 6. v0.1 进度

| 状态 | 条目 |
|---|---|
| 已点亮 | AC03（R1）· **AC01、AC02、AC26（R2）** |
| 合计 | **4 / 30** |

R1 后为 1/30，R2 后为 4/30。剩余 26 条中 AC27、AC28 **仍无承接轮次**——§5 与 §7 均维持「由 Human 在后续控制面归一化」。这是 R2 期间唯一没有推进的治理项，建议在 R3 开工前于 `layering-refactor.md` 迭代序列表中落位（我仍倾向 R4）。

---

## 7. 本报告的局限

- 我未执行任何命令。§5「仍待复核」的条目为未知，非通过；「已由 Human 复核完毕」的三项由 Human 以本地 Git 与实际门禁运行确认，不是我的证据。
- 无 git：base-to-candidate 的整体写路径 diff 与门禁脚本 diff 仍无法由我核验。
- 「规则值已迁移」我核验的是**当前 pack 内容与迁移前数值逐个相同**，不是**计算路径确实读取了它们**——后者由 §6 的 gRPC-Web 对照切片承担，需 Human 复核。
- 哈希类断言（冻结事实、canonical schema）我只能核对登记值的**存在与一致**，无法重算文件内容的 SHA-256。
