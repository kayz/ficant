# 历史迭代 brief（Phase 0–5A）

本目录保存 2026-07-26 分层重构决定之前的 23 篇迭代 brief。它们是**当时的证据**，不再驱动当前工作。

## 状态

| brief 覆盖范围 | 重构后状态 |
|---|---|
| Phase 2C 国债期货交割（`2026-07-phase2c-futures-delivery.md`） | **取证将失效。** R2 把 CFFEX 规则数值移入 RulePack 后必须重跑 |
| Phase 3A/3B 数据接入与快照（`2026-07-phase3a-*`、`2026-07-phase3b-*`） | **取证将失效。** R4 扩展 canonical quote schema（来源类型与可信度）后必须重跑，schema 哈希会改变 |
| Phase 2A/2B/2D/2E 数值与 SDK | 计算语义不变，但 R3 引入主体绑定后调用形态改变，接口层证据需补 |
| Phase 4 执行运行时 | 执行语义不变，R1 起为加法式变更，证据继续成立 |
| 交付、供应链、CI/CD、镜像相关（`*-cicd-*`、`*-supply-chain-*`、`*-image-*`、`*-cve-*`、`*-ceph-*`、`*-storage-runtime-*`） | 与领域分层无关，证据继续成立 |
| Phase 5A 只读观测面板 | 界面绑定的对象模型将变。原 Phase 5 已后移至 R7 之后 |

## 为什么保留而不删除

这些 brief 记录了**不可再生知识**：实测的外部行为、踩过的坑、当时的真实命令与退出码。重构会使其中一部分结论失效，但不会使其中的教训失效。

## 权威

判定权威在仓库根目录的 `SPEC.md` 与 `ACCEPTANCE.md`。本目录任何内容与之冲突时，以后者为准；本目录不得被引用为对重构后系统的保证。
